# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...
# Optional: which Claude model writes the cards (default claude-opus-5)
# CLAUDE_MODEL=claude-opus-5
# Optional locally, required on Vercel: the password your group types to get in
# APP_PASSWORD=
